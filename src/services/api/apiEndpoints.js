
const BASE_URL = 'https://your-api-url.com/api'; // Replace with your real backend URL

const API_ENDPOINTS = {
  appointments: `${BASE_URL}/appointments`,
  login: `${BASE_URL}/auth/login`,
  register: `${BASE_URL}/auth/register`,
  user: `${BASE_URL}/user`,
  // Add more as needed
};

export default API_ENDPOINTS;
